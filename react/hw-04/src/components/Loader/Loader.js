import LoaderOverlay from "components/styled/LoaderOverlay";
import Spin from "components/styled/Spin";

const Loader = () => (
  <LoaderOverlay>
    <Spin />
  </LoaderOverlay>
);

export default Loader;
